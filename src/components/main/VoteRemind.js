import React from 'react'
import { View, Text } from 'react-native'

const VoteRemind = props => {
    return (
        <View>
            <Text>VoteRemind</Text>
        </View>
    )
}

export default VoteRemind
